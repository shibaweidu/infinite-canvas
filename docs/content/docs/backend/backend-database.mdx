---
title: 数据库说明
description: 当前后端主要数据表与字段说明
---

# 数据库说明

本文档只记录后端当前已经使用的主要数据表。

## 数据库

后端使用 GORM 管理数据库连接和表结构迁移。

支持的存储驱动：

- `sqlite`
- `mysql`
- `postgresql`

当前启动时执行 `AutoMigrate`，自动维护以下表：

- `users`
- `credit_logs`
- `credit_batches`
- `prompts`
- `assets`
- `settings`
- `announcements`
- `subscription_plans`
- `credit_packages`

后续新增表时再同步补充本文档，未实际使用的规划表不提前写入。

### users

系统用户表。用户基础信息、角色、积分余额和第三方登录标识放在该表中。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | 主键 |
| `username` | string | 用户名，唯一索引 |
| `password` | string | 密码哈希 |
| `email` | string | 邮箱 |
| `display_name` | string | 昵称 |
| `avatar_url` | string | 头像地址 |
| `role` | string | 角色：`user`、`admin` |
| `credits` | number | 积分余额缓存，来自未过期积分批次的剩余积分合计 |
| `aff_code` | string | 用户自己的邀请码，唯一索引 |
| `aff_count` | number | 已邀请用户数量，冗余统计字段 |
| `inviter_id` | string | 邀请人用户 ID |
| `github_id` | string | GitHub 用户 ID |
| `linux_do_id` | string | Linux.do 用户 ID |
| `google_id` | string | Google 用户 ID |
| `wechat_id` | string | 微信用户 ID |
| `status` | string | 用户状态：`active`、`ban` |
| `last_login_at` | string | 最近登录时间 |
| `extra` | json | 扩展信息，第三方资料按平台命名空间保存，如 `linuxDo` |
| `created_at` | string | 创建时间 |
| `updated_at` | string | 更新时间 |

### prompts

提示词表。用于保存公开提示词、内置 GitHub 系统提示词、分类和预览内容。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | 主键 |
| `title` | string | 标题 |
| `cover_url` | string | 封面图 |
| `prompt` | string | 提示词内容 |
| `tags` | json | 标签列表 |
| `category` | string | 分类标识 |
| `preview` | text | Markdown 展示内容，可包含文本、图片、视频链接等 |
| `created_at` | string | 创建时间 |
| `updated_at` | string | 更新时间 |

`github_url` 仅用于接口返回，不写入数据库。

### assets

素材表。当前用于后台素材库。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | 主键 |
| `title` | string | 标题 |
| `type` | string | 素材类型：`text`、`image`、`video` 等 |
| `cover_url` | string | 封面图 |
| `tags` | json | 标签列表 |
| `category` | string | 分类标识 |
| `description` | string | 描述 |
| `content` | text | 文本或 Markdown 内容 |
| `url` | string | 图片、视频等媒体地址 |
| `created_at` | string | 创建时间 |
| `updated_at` | string | 更新时间 |

### announcements

公告表。用于保存前台公告列表和后台发布、置顶、显示控制。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | 主键 |
| `title` | string | 公告标题 |
| `summary` | text | 摘要 |
| `content` | text | 正文 |
| `pinned` | bool | 是否置顶 |
| `enabled` | bool | 是否前台显示 |
| `sort` | number | 排序值 |
| `published_at` | string | 发布时间 |
| `created_at` | string | 创建时间 |
| `updated_at` | string | 更新时间 |

### subscription_plans

订阅套餐表。用于后台维护前台展示的订阅套餐和积分规则。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | 主键 |
| `name` | string | 套餐名称 |
| `description` | text | 套餐说明 |
| `price` | number | 价格，单位为分 |
| `original_price` | number | 前台展示原价，单位为分，可为空 |
| `credits` | number | 套餐包含积分 |
| `duration_days` | number | 有效天数 |
| `price_cycle` | string | 前台展示价格周期，例如 `每月` |
| `button_text` | string | 前台按钮文案 |
| `credit_label` | string | 前台积分模块文案 |
| `credit_rate_text` | string | 前台积分换算说明 |
| `benefits` | json | 前台权益列表，字段为 `text` 和 `tag` |
| `enabled` | bool | 是否启用 |
| `sort` | number | 排序值 |
| `created_at` | string | 创建时间 |
| `updated_at` | string | 更新时间 |

### credit_packages

积分充值包表。用于后台维护前台展示的积分充值规则。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | 主键 |
| `name` | string | 充值包名称 |
| `description` | text | 充值包说明 |
| `price` | number | 价格，单位为分 |
| `original_price` | number | 前台展示原价，单位为分，可为空 |
| `credits` | number | 基础积分 |
| `bonus_credits` | number | 赠送积分 |
| `price_cycle` | string | 前台展示价格周期，例如 `一次性` |
| `button_text` | string | 前台按钮文案 |
| `credit_label` | string | 前台积分模块文案 |
| `credit_rate_text` | string | 前台积分换算说明 |
| `benefits` | json | 前台权益列表，字段为 `text` 和 `tag` |
| `enabled` | bool | 是否启用 |
| `sort` | number | 排序值 |
| `created_at` | string | 创建时间 |
| `updated_at` | string | 更新时间 |

### credit_batches

用户积分批次表。用于区分订阅积分、充值积分、赠送积分、后台调整和失败返还。订阅积分可设置过期时间；充值积分和赠送积分不设置过期时间。消费积分时先扣未过期且最早过期的批次，再扣永久批次。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | 主键 |
| `user_id` | string | 用户 ID |
| `source_type` | string | 来源：`subscribe`、`recharge`、`bonus`、`admin`、`refund` |
| `source_id` | string | 来源业务 ID，例如套餐 ID、充值包 ID 或后台调整标识 |
| `total_credits` | number | 批次总积分 |
| `remaining_credits` | number | 批次剩余积分 |
| `expires_at` | string | 过期时间；为空表示永久有效 |
| `created_at` | string | 创建时间 |
| `updated_at` | string | 更新时间 |

### settings

系统配置表，只保存两行数据：`public` 放前端可读取的公开配置，`private` 放仅后端和管理员可读取的私有配置，配置值都用 JSON。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `key` | string | 主键：`public`、`private` |
| `value` | json | 配置内容 |
| `created_at` | string | 创建时间 |
| `updated_at` | string | 更新时间 |

`public.value` 常放前端展示和可公开读取的配置，例如模型列表、登录开关等。  
`private.value` 常放渠道密钥、登录密钥、后台内部开关等。

当前系统设置接口会按后端结构体序列化和反序列化已知字段；数据库 JSON 中额外存在的旧字段会被忽略。

`public.value` 当前字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `modelChannel` | object | 模型渠道公开配置组 |
| `auth` | object | 公开登录配置 |
| `objectStorage` | object | 对象存储公开配置 |

`modelChannel` 当前字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `availableModels` | string[] | 系统可用模型列表 |
| `modelCosts` | object[] | 模型积分配置 |
| `defaultModel` | string | 默认模型 |
| `defaultImageModel` | string | 默认图片模型 |
| `defaultVideoModel` | string | 默认视频模型 |
| `defaultTextModel` | string | 默认文本模型 |
| `systemPrompt` | string | 系统提示词 |
| `allowCustomChannel` | bool | 是否允许用户自定义渠道，默认允许，关闭后前端只提供走后端渠道的模式 |

`modelCosts` 每项字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `model` | string | 模型名称 |
| `name` | string | 模型展示名称 |
| `type` | string | 模型分类：`text`、`image`、`video`、`audio` |
| `thumbnailUrl` | string | 模型缩略图地址 |
| `providerDisplayName` | string | 展示用供应商名称 |
| `description` | string | 模型描述 |
| `tags` | string[] | 模型标签 |
| `credits` | number | 文本模型每次调用扣除；图片/视频模型未命中细分规则时作为兜底 |
| `resolutionCosts` | object[] | 图片模型按分辨率扣费，字段为 `resolution` 和 `credits` |
| `secondCredits` | number | 视频模型每秒扣除的积分 |

`auth.linuxDo` 当前字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `enabled` | bool | 是否开启 Linux.do 登录 |

`auth.emailRegister` 当前字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `enabled` | bool | 是否开启邮箱/账号密码注册 |
| `emailRequired` | bool | 注册时邮箱是否必填 |
| `codeEnabled` | bool | 是否开启注册邮箱验证码；开启后注册必须先发送并填写验证码 |

`auth.google` 当前字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `enabled` | bool | 是否开启 Google 登录 |

`objectStorage` 公开字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `enabled` | bool | 是否开启对象存储 |
| `provider` | string | 存储供应商标识 |
| `bucket` | string | Bucket 名称 |
| `region` | string | Region |
| `publicUrl` | string | 公开访问地址 |

`private.value` 当前字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `channels` | object[] | 模型渠道配置列表 |
| `promptSync` | object | GitHub 远程提示词定时同步配置 |
| `auth` | object | 私有登录配置 |
| `objectStorage` | object | 对象存储私有连接配置 |

`channels` 每项字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `protocol` | string | 协议，当前支持 `openai` |
| `name` | string | 渠道名称 |
| `baseUrl` | string | 渠道接口地址 |
| `apiKey` | string | 渠道密钥 |
| `hasApiKey` | bool | 后台读取配置时返回的密钥状态标记，用于提示已配置密钥但不暴露密钥内容 |
| `models` | string[] | 渠道已启用模型列表，由 `modelItems` 中启用项同步生成 |
| `modelItems` | object[] | 渠道模型明细，包含模型展示信息、分类、选择状态、启用状态、计费配置和 API 路由 |
| `weight` | number | 渠道权重，同一模型命中多个渠道时按权重随机 |
| `enabled` | bool | 是否启用 |
| `remark` | string | 备注 |

`modelItems` 每项字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `model` | string | 模型 ID |
| `name` | string | 模型展示名称 |
| `type` | string | 模型分类：`text`、`image`、`video`、`audio` |
| `selected` | bool | 是否选入“模型选择”列表；只有已选择模型会进入积分设置 |
| `enabled` | bool | 是否启用给前台；只有已选择且启用的模型会进入公开可选模型 |
| `thumbnailUrl` | string | 模型缩略图地址，可保存上传后的 data URL |
| `providerDisplayName` | string | 前台或后台展示用供应商名称 |
| `description` | string | 模型描述 |
| `tags` | string[] | 模型标签 |
| `credits` | number | 文本模型每次调用扣除；图片/视频模型未命中细分规则时作为兜底 |
| `resolutionCosts` | object[] | 图片模型按分辨率扣费，字段为 `resolution` 和 `credits` |
| `secondCredits` | number | 视频模型每秒扣除的积分 |
| `apiRoutes` | object[] | API 路由选项，字段为 `path` 和 `enabled` |

当前支持的 `apiRoutes.path`：

- 文本：`/chat/completions`
- 图片：`/images/generations`、`/images/edits`、`/chat/completions`、`/responses`、`/v1/async/generations`、`/v1/videos`
- 视频：`/chat/completions`、`/video/generations`、`/v1/video/create`、`/videos`、`/v1/async/generations`、`/async/generations`、`/video/create`
- 音频：`/audio/speech`

`promptSync` 字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `enabled` | bool | 是否开启定时同步，默认开启 |
| `cron` | string | Cron 表达式，默认每 5 分钟 |

`auth.linuxDo` 当前字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `clientId` | string | Linux.do OAuth App Client ID |
| `clientSecret` | string | Linux.do OAuth App Client Secret，后台返回时隐藏 |

`auth.email` 当前字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `email.smtpHost` | string | SMTP 服务地址 |
| `email.smtpPort` | number | SMTP 端口，默认 `587` |
| `email.smtpUsername` | string | SMTP 用户名 |
| `email.smtpPassword` | string | SMTP 密码，后台返回时隐藏 |
| `email.fromEmail` | string | 发件邮箱 |
| `email.fromName` | string | 发件人名称 |
| `email.subject` | string | 验证码邮件标题 |

`auth.google` 当前字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `clientId` | string | Google OAuth Client ID |
| `clientSecret` | string | Google OAuth Client Secret，后台返回时隐藏 |

`objectStorage` 私有字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `enabled` | bool | 是否开启对象存储 |
| `provider` | string | 存储供应商标识 |
| `endpoint` | string | S3 兼容 Endpoint |
| `region` | string | Region |
| `bucket` | string | Bucket 名称 |
| `accessKeyId` | string | Access Key ID |
| `secretAccessKey` | string | Secret Access Key，后台返回时隐藏 |
| `publicUrl` | string | 公开访问地址 |
| `prefix` | string | 文件路径前缀 |
| `forcePathStyle` | bool | 是否使用 path-style 访问 |

后端请求模型时，先按模型名筛选启用且包含该模型的渠道，再按 `weight` 加权随机选择一个渠道。

### credit_logs

用户积分变更流水表。当前记录后台手动调整、模型调用预扣和模型调用失败返还。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | 主键 |
| `user_id` | string | 关联用户 ID |
| `type` | string | 类型：`admin_adjust`、`ai_consume`、`ai_refund` |
| `amount` | number | 本次变动数量，增加为正，扣减为负 |
| `balance` | number | 变动后的用户积分余额 |
| `related_id` | string | 关联业务 ID，可为空 |
| `remark` | string | 备注 |
| `extra` | json | 扩展信息 |
| `created_at` | string | 创建时间 |

`type` 当前取值：

| 值 | 说明 |
| --- | --- |
| `admin_adjust` | 后台手动调整 |
| `ai_consume` | 调用后端模型接口消费 |
| `ai_refund` | 后端模型接口调用失败返还 |
